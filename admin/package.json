{
  "name": "zekierman-admin",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "The panel that adds a project to zekierman.me. Runs on the VPS beside the site; writes content files, not a database.",
  "scripts": {
    "start": "node server.mjs",
    "set-password": "node set-password.mjs"
  },
  "dependencies": {
    "@fastify/cookie": "^11.0.2",
    "@fastify/multipart": "^9.0.3",
    "@fastify/static": "^8.1.1",
    "fastify": "^5.2.1"
  }
}
